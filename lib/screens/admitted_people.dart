import 'package:flutter/material.dart';

class AdmittedPeopleScreen extends StatelessWidget {
  const AdmittedPeopleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Admitted People"),
    );
  }
}
