import 'package:flutter/material.dart';

class AddPeopleScrenn extends StatelessWidget {
  const AddPeopleScrenn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Center(
          child: Text("Add People Screnn"),
        ));
  }
}
